## Config

# Install
  `yarn install`
  `yarn install --dev`

# Run Webpack
  `yarn run dev-server`

# Babel
  `babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch`

# test
  `yarn test -- --watch`
